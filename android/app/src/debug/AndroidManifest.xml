<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug build type: attach the cleartext-to-loopback network-security config so the
  local pack-demo server is reachable in debug. Release builds get no such override.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
