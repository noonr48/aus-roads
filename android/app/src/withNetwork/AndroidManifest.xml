<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- withNetwork flavor: INTERNET for map pack download. Location for "My Location" button. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Location permissions for "My Location" button and navigation GPS -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Location-type foreground service that keeps navigation GPS alive while
         the screen is off or the app is backgrounded (API 34+ requires both). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Android Auto descriptor names a com.google.android.gms.* component, so it is
         declared only in the withNetwork flavor to keep the privacy-first offline
         build free of any Google Play Services reference (incl. the manifest). -->
    <application>
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- WorkManager runs the map-pack download as a foreground service. On API 34+
             the service must declare a foregroundServiceType matching the runtime type
             (dataSync) and the FOREGROUND_SERVICE_DATA_SYNC permission above; without
             this the download worker crashes with "foregroundServiceType 0x1 is not a
             subset of 0x0". Confined to withNetwork (only this flavor downloads). -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Keeps a turn-by-turn session's GPS updates alive while the screen is
             off or the app is backgrounded. The manifest type must match the
             runtime FOREGROUND_SERVICE_TYPE_LOCATION promotion in
             NavigationForegroundService. Confined to withNetwork (only this
             flavor has location code). -->
        <service
            android:name="au.com.ausroads.navigation.NavigationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
